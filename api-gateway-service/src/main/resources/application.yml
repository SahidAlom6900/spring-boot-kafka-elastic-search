server:  port: 8765  eureka:  client:    register-with-eureka: true    fetch-registry: true  instance:    hostname: localhost    

spring:
  application:
    name: api-gateway-service      cloud:    gateway:      routes:      - id: product-service        uri: lb://product-service        predicates:        - Path=/api/v1/*                
    
    